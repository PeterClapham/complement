experiment:
  name: variational_gon_seed0_batch128_80ep
  results_dir: results
seeds:
  - 0
datasets:
  - name: mnist
    data_dir: data/mnist
    split: train
    download: true
    image_size: 32
    binarize: false
    cache_tensors: true
    validation_fraction: 0.0
model:
  name: variational_gon
  latent_dim: 48
  base_channels: 32
  output_channels: 1
training:
  batch_size: 128
  device: cuda
evaluation:
  batch_size: 128
  device: cuda
  output_dir: results/evaluation/mnist_seed0_80ep
  checkpoint_epochs: [50, 60, 70, 80]
betas:
  values: [0.01, 0.05, 0.1, 0.5, 1.0, 5.0, 10.0]
